{
  "name": "engrams-site",
  "private": true,
  "license": "AGPL-3.0-only",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm gen && astro dev",
    "lint": "node scripts/check-leaks.mjs",
    "check": "astro check",
    "build": "pnpm gen && pnpm lint && astro check && astro build",
    "preview": "astro preview",
    "gen": "node scripts/gen-api.mjs"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.11",
    "@fontsource-variable/jetbrains-mono": "^5.3.0",
    "@fontsource-variable/saira": "5.3.0",
    "astro": "^7.2.10",
    "sharp": "^0.35.0",
    "starlight-links-validator": "^0.25.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "typescript": "^5.9.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp"
    ]
  }
}
