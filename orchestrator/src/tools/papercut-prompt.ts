export const PAPERCUT_SYSTEM_PROMPT = `## Papercuts
While working, when you hit friction outside your main task—confusing errors, missing or awkward tooling, docs gaps, slow or flaky commands, or environment quirks you had to work around—log it with the \`papercut\` tool.
Make one short call, then continue the main task; never block or derail it for a papercut.
Never log the same papercut twice in a session.
Never include secrets, tokens, or credentials in papercut text.`;
