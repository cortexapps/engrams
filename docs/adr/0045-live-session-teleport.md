# ADR 0045: The unified memory substrate — one guest-memory architecture for density, lazy restore, live teleport, and fast teardown

Status: 2026-06-09 — **Proposed → partially Accepted** (rewritten around the end-state substrate; see "What changed in this rewrite"). Landed: Phase A (retire reactive evac, #136), Phase F's teleport test surface (snapshot-rehome form, #137), Phase E1's scale-down decision engine (#138), and **Phase B — the fork itself**: ported onto upstream FC, vendored, drop-in-proven, self-maintaining via the tracking cron, caught up to **v1.16.0**, and **rolled to prod** (#139/#149/#151–#153/#158/#159). **Phase D — the unified memory substrate — is COMPLETE and prod-validated** (D1–D5 + seed-at-create; see the D-ladder bookends). What remains: **Phase C — live teleport on the substrate** (staged C1 eager push → C2 post-copy; Proposed, in flight), then the E1 actuator + E2 and Phase F's live path. Flips fully to **Accepted** phase-by-phase as the commit chain lands. Supersedes ADR 0043 Phase 3b (retire evac) and carries forward ADR 0042's Tier-3 "differentiated move."

## What changed in this rewrite (2026-06-09)

The original ADR framed the golden state as two loosely-coupled fork features: `MAP_SHARED` off-pause flush (old Phase D) and post-copy migration (Phase C). Design review against the running system found the old Phase D **does not compose with production**, and that chasing it piecemeal would have either regressed density or forked the memory path per lifecycle op:

- **The old Phase D's mechanism was unimplementable as written.** `Msync` flushes file-backed `MAP_SHARED` guest memory — but no prod session has that. UFFD restores build guest RAM from *anonymous* pages (`UFFDIO_COPY` installs private copies); File restores map the *immutable base* memfile `MAP_PRIVATE`, so guest writes COW to anonymous pages msync never sees. The old coexistence claim ("different inodes, opposite directions — no interaction") hid a missing object: a live per-sandbox memfile that nothing creates. Creating one naively either lands a multi-GB materialization on the create/resume latency path or — by giving each sandbox a private file — silently retires the page-cache density win that ADR 0022/0039 shipped to prod.
- **Resume has a hidden density loss today.** Fresh creates share clean base pages across same-template siblings (`MAP_PRIVATE` of one resident memfile: 3 siblings measured Σpss/Σrss = 35%). Idle **resumes don't**: every UFFD-installed page is a private copy, even the large majority still byte-identical to the base. The original ADR never named this.
- **The kernel primitive matrix pins the end-state design** (details under Decision): UFFD `MINOR`/`WP` modes require shmem; file-backed (ext4) mappings support neither; KSM is anonymous-only; msync-to-disk requires a regular file. The construct that yields shared-clean + private-dirty + lazy-from-anywhere in a single mapping is `MAP_PRIVATE` of a shm file under UFFD `MISSING|MINOR` (the S0.6 finding — v2b), and it is the substrate this ADR now commits to.

So the rewrite replaces "two fork features" with **one memory architecture** that cold boot, idle resume, teardown, and teleport all run on — and re-derives Phases C/D from it. Phases A/B/E/F and all shipped history are unchanged. Interim shortcuts found during the review (a `process_vm_readv` teleport source that needs no fork; a teardown pipeline reorder on the existing diff-chain) are recorded as de-risk fallbacks, explicitly not the plan of record.

## Context

The substrate has evolved across three ADRs:
- **ADR 0042** (survey) named the differentiated move: **post-copy live migration with a durable-snapshot fallback** — "teleport" a running session host-to-host with minimal downtime, crash-safe because the working set is already durable in GCS. That intersection is unoccupied: silo/Drafter do post-copy *without* a durable fallback; everyone else (E2B, CodeSandbox, Modal, Replit) does snapshot-and-rehome *without* post-copy. Our durable chunk store gives us both.
- **ADR 0043** (roadmap) shipped the no-fork wins — P0 fail-fast, P1 immediate-resume / non-blocking UFFD prefetch, P2a relaxed checkpoint cadence — and explicitly gated the fork.
- **ADR 0044** (shipped) moved the Firecracker host fleet to Kubernetes. Two consequences land here: (1) the **drain-driven evacuation machinery is now load-bearing** — K3 drain = mark `Evacuating` → the `evac_resumer` scanner → resume on a peer — so ADR 0043's "fully retire session evac" can no longer be taken literally; and (2) ADR 0044 K4 shipped demand autoscaling but **deferred drain-gated scale-*down***.

And the memory paths in production today, which this ADR unifies:
- **Fresh create (cold boot)** = `RestoreMode::File`: `MAP_PRIVATE` of the resident per-template base memfile (ADR 0022 Option A, promoted to prod default by ADR 0039). ~60 ms restore, no handler, and density — clean base pages shared across same-template siblings through the page cache.
- **Idle resume** = `RestoreMode::Uffd`: the `engram-uffd-handler` lazily installs 512 KiB chunks via `UFFDIO_COPY` into anonymous guest RAM, racing a working-set prefault (ADR 0043 P1). Cross-host-capable, but every installed page is private — no sharing, even for base-identical pages.
- **Teardown / checkpoints** = the diff-chain (ADR 0028/0038/0039): periodic `Diff` captures at 38–53 ms guest pause, sparse re-chunk, 32-way parallel GCS upload. Durability is bounded-loss by cadence.

What remains to reach the golden state, and what this ADR commits to:
1. **Retire the *reactive* evac, keep the *drain-driven* evac** (shipped, Phase A). The drain-driven path is the seam Phase C upgrades.
2. **Fork Firecracker** (shipped, Phase B) — now scoped as the enabler of the substrate: stock FC cannot map guest memory from a shared shmem object nor register UFFD beyond `MISSING` mode.
3. **The unified memory substrate** (Phase D, rewritten) — one guest-memory architecture giving density + lazy restore + external readability (via `process_vm_readv` on a paused FC), replacing the File/Uffd split; dirty tracking stays the proven KVM-dirty-log diff chain.
4. **Post-copy live teleport on the substrate** (Phase C, rewritten) — the headline: live teleport, crash-safe, with near-nothing to do at handoff because the substrate already separates base (resident everywhere) from divergence (private COW pages + the diff-chain's chunks).
5. **True autoscale-down** (Phase E) — the payoff that rides the teleport seam: the fleet continuously self-compacts to its cost-optimal node count with minimal perceived disruption.

The unifying insight, sharpened by the rewrite: **the expensive thing in every lifecycle operation — boot, resume, checkpoint, teardown, teleport — is moving or copying guest memory. Make guest memory an explicitly-structured object (shared base + private divergence) and every operation becomes a cheap manipulation of that structure** instead of a bulk copy: boot = map the base; resume = map the base + lazily fill the divergence; checkpoint/teardown = capture only the dirty set; teleport = move only the divergence. We don't build five mechanisms; we build one substrate and five thin consumers.

## Decision

**The substrate (v2b — the design of record after the S0.6 cross-process probe; see the S0 results under Phase D).** Every session VM's guest memory — cold boot, idle resume, post-teleport destination — is built the same way:

```
guest RAM = MAP_PRIVATE of the per-template BASE shm file    (one per host; lazily populated; clean pages
            registered UFFD MISSING | MINOR                   shared by all same-template sessions; guest
                                                              writes COW natively to private anon pages)
```

- **Read fault on a base-identical page** → the handler resolves it `UFFDIO_CONTINUE` against the shared page cache: zero copy, one physical page serves every same-template session on the host. This is File-mode's density **extended to resumes** — strictly better than today, where resume privately copies everything.
- **Session-divergent pages** (resume / post-teleport) → the handler installs them with `UFFDIO_COPY` — private to the sandbox, the shared base file untouched. This maps one-to-one onto the handler's existing resolution enum: `ResolvedPage::Canonical` → CONTINUE, `ResolvedPage::Chunk` → COPY.
- **Guest writes** → kernel-native COW to private anonymous pages: privacy with **zero handler involvement, zero new fork surface, zero VMA growth**. (The first-round MAP_SHARED + WP + overlay-carve variant also passed every in-process probe, but its `mmap(MAP_FIXED)` carve cannot cross the FC/handler **process boundary** — production's handler is a separate process. It is kept as the documented fallback; see Interim fallbacks.)
- **Base population is lazy and shared**: on a `MISSING` fault for an unpopulated base page, the handler `pwrite`s the canonical chunk into the base shm file **by path** (visible to every sibling), then `CONTINUE`s. Hole-punch reclaims cold base regions under pressure. Only canonical base content is ever written to the base file.
- **Resume / teleport destination**: divergent pages fill lazily from the session's chunk manifests via COPY. Phase C's two-source race (peer host vs GCS) plugs in at exactly this fill point. Base pages never transfer at all — only divergence moves.
- **Teardown / checkpoint**: the shipped diff-chain, unchanged — KVM dirty-log `Diff` captures (38–53 ms pause) + sparse re-chunk + parallel upload; D5 reorders the pipeline so the user-visible teardown ends at the capture, with upload in the background.
- **Teleport source**: the base already exists on every host; the destination needs only the divergence (raced against GCS) plus `process_vm_readv` on the paused source FC for pages not yet durable. Near-nothing happens at handoff.

**One path, retired split.** `RestoreMode::{File,Uffd}`, `effective_restore_mode(fresh)`, and `ENGRAM_FC_BASE_RESTORE_MODE` retire once the substrate passes its parity gates (Phase D3/D4) — a clean break, not a third mode living alongside two old ones.

**Why this construct and not something simpler — the kernel primitive matrix.** The requirements are: (a) clean base pages physically shared across sessions (density); (b) dirty pages private; (c) pages fillable lazily from arbitrary sources (chunks, peer host, GCS); (d) current memory readable from outside the VMM (teleport source). Against the kernel's actual capabilities:
- File-backed (ext4) `MAP_PRIVATE` gives (a)+(b) — today's File mode — but supports **no** UFFD interception, so (c) is impossible: it only works when the complete image already sits on local disk. That is precisely why resume can't use it.
- Anonymous + UFFD `MISSING` gives (c) — today's Uffd mode — but every installed page is private: no (a).
- `UFFDIO_MINOR`/`UFFDIO_CONTINUE` exist **only on shmem/hugetlbfs** (MINOR ≥5.13). KSM is anonymous-only. msync-to-disk needs a regular file (tmpfs has no writeback) — which is why the old Phase D's `Msync` design was unimplementable against any UFFD-capable backing.
- **The unlock (S0.6): `MISSING|MINOR` registration, cross-process `CONTINUE`, and `UFFDIO_COPY` are all legal on a `MAP_PRIVATE` mapping of a shm file.** That single fact composes everything: shmem backing makes the mapping UFFD-interceptable (c); `MAP_PRIVATE` + `CONTINUE` shares clean page-cache pages (a) while kernel-native COW keeps writes private (b); `process_vm_readv` on the paused FC gives (d). No WP, no carve, no per-sandbox overlay file. This is — derived clean-room from primitives, never from the AGPL implementations — File mode and Uffd mode collapsed into one mapping.

**Honest bounds.**
- *Downtime*: post-copy handoff = pause + `state.bin` move + destination resume; the destination's bring-up (FC spawn, netns, load) dominates the first iteration and is amortized by **pre-staging** (Phase C). Budget claims are deferred to measurement (S0.4 + Phase C kill-tests) — the old ~7–12 ms figure was QEMU's, not ours.
- *Crash-safety is bounded, not transactional*: recovery is from the last durable flush; loss ≤ one checkpoint interval. The periodic checkpoint (ADR 0043 P2a) stays the always-on backstop; it is *the* thing that makes our post-copy crash-safe where the field's is not.
- *Density is a hard gate, not a hope*: Phase D3/D4 may not ship unless measured density on real images is ≥ today's File mode and restore latency ≤ today + ε.
- *New standing costs (v2b — much smaller than the carve variant's)*: the base shm file's pages are swappable but not page-cache-reclaimable the way today's memfile cache is (size it per-template, hole-punch cold regions); dirty guest pages are anonymous COW — identical RAM accounting to today. Kernel floor is MINOR-on-shmem ≥5.13 (GKE COS 6.12 ✓, dev VM 6.8 ✓). No VMA growth, no `max_map_count` concern (those were carve-variant costs).

**Key sub-decisions:**

1. **Reconcile "retire evac" rather than delete it wholesale** (shipped, Phase A). The `Evacuating` state + `evac_resumer` scanner + `evacuate_dead_source` are the K3-drain engine; only the *reactive* producers were retired.

2. **Fork Firecracker, minimal surface, ported onto upstream** (shipped, Phase B). The fork's v2 surface (Phase D1) is small: on the Uffd restore path (and eventually boot), construct guest memory as `MAP_PRIVATE` of a configured base shm file instead of anonymous, and register UFFD `MISSING|MINOR` instead of MISSING-only — plus a load-request field carrying the base path. The v1 `Msync`/`MsyncAndState`/`shared` surface is superseded by the substrate (tmpfs has no writeback to msync) and will be repurposed or retired honestly in D1. The invariants stand: never touch `src/vmm/src/snapshot/`, never bump `SNAPSHOT_VERSION` (the R2 byte-compat guard), keep the delta a small auditable commit series, stay clean-room.

3. **Reuse the fill seam.** The substrate keeps the `engram-uffd-handler` as the single fault authority, and its existing resolution enum maps directly onto the new ioctls (`Canonical` → CONTINUE, `Chunk` → COPY); Phase C's migration mode stays a producer swap at the fill point (race `{p2p.need_at, store.get_chunk}`, first SHA-256-verified wins), exactly as the original ADR planned at `chunked.rs`' `cache.get(hash, || store.get_chunk(hash))` — the surrounding singleflight / idempotency / hash-verify machinery is reused, not rebuilt.

4. **Crash-safety is the tested default, not a catch block.** GCS is a co-equal, always-armed parallel arm of the *durable* divergence fill (Phase C's class 2); for pages dirtied since the last checkpoint GCS has no sound copy, so their crash arm is the rung-1 whole-VM rewind via the existing coherent-checkpoint recovery (see Phase C's three-class taxonomy). Kill-tests at every migration phase gate Phase C — in CI.

5. **Go directly to the end-state; record the shortcuts as fallbacks.** The review found two cheaper interim paths (below, "Interim fallbacks"). We are explicitly **not** taking them as the roadmap — the substrate is the point of carrying the fork — but they are documented and available if the S0 spikes kill the design.

## Phases

| Phase | What | Fork? | Status |
|---|---|---|---|
| **A** | Retire *reactive* evac (dead-host auto-evac + NBD-loss); keep drain-driven evac; lazy-resume-on-next-access | no | ✅ **shipped** — #136 |
| **B** | Fork Firecracker — vendored + auto-rebased + detect-changes-wired; v1 surface (`MAP_SHARED`+`Msync`) superseded by D1's v2 surface | — | ✅ **done + prod-rolled at v1.16.0** (#139/#149/#151–#153/#158/#159) |
| **D** | **The unified memory substrate** — `MAP_PRIVATE` of the shared base shm + UFFD `MISSING\|MINOR` + native COW (v2b); one restore path; teardown pipeline reorder | yes (v2) | ◐ **S0 spikes ALL PASS incl. S0.6** (v2b frozen as the design of record); D1 fork v2 next |
| **C** | Post-copy live teleport **on the substrate** (two-source overlay fill + durable GCS fallback) | yes | ⬜ pending — gated on D3 |
| **E** | True autoscale-down — E1 (no-fork, idle-only) now; E2 (aggressive) after C | E2 only | ● **actuator SHIPPED via ADR 0048** (teleport-packed wave + `remove_node`; E1+E2 modes); prod rollout staged |
| **F** | Admin + web UX for pause / resume / teleport — the test surface; snapshot-rehome early, live under the same verb at C | no | ◐ **teleport shipped** (#137); pause/resume + live-path swap pending |
|  |  |  |  |

**Dependencies:** `B (✅) → S0 (✅) → D1 → D2 → D3 → C`; D4/D5 follow D3 and parallelize with C's build-out. A is independent and shipped. E1 and F's snapshot-rehome form are fork-independent (shipped); E2 and F's live path are config/impl upgrades once C lands. **S0 was the decision gate and it passed clean** — including S0.3 (carve under a live KVM memslot, reads-don't-carve), the one assumption that could have killed the design.

## Phase detail

### Phase A — retire reactive evac (no fork)

**Status: ✅ shipped (#136).** Landed exactly as designed below — `dead_host.rs` routes recoverable sessions to `Idle`, `nbd_loss_trigger.rs` + the `nbd_unhealthy` wire field are gone, and the live-PG routing test guards it.

Retire the reactive evac, keep the drain-driven evac. **No DB migration** (`Evacuating` + `host_lost` stay valid `sessions.status` values; nothing is removed). **No new resume code** — routing a dead host's recoverable sessions to `Idle` lands them on the existing `/resume` path (`resume_session` → `resume_from_idle`).

- **`dead_host.rs`** — drop `auto_evac_enabled()` + the `ENGRAM_DEAD_HOST_AUTO_EVAC` flag; the detector's stage-2 routing becomes `snapshot.is_some() || has_live_manifest → Idle` (lazy resume; preserves disk-only recoverability via `resume_disk_only_cold_boot`), else `Dead`. The detector itself stays — sessions must not linger `Active` pointing at a dead host.
- **Delete `nbd_loss_trigger.rs`** + its `pub mod` + the heartbeat hook, and remove the now-always-empty `nbd_unhealthy` wire field across `engram-protocol` / `engram-coordinator` / `engram-host-agent` (incl. the never-wired `NbdHealthMonitor`) as one atomic cross-crate commit. The field is `#[serde(default)]` everywhere, so the roll is order-independent.
- **Keep untouched:** `evac_resumer` (the scanner), `evacuate_dead_source` + helpers (still the scanner's relocation primitive), `admin.rs` drain/evacuate/cordon/uncordon, the `HostLost → Evacuating` transition edge (harmless, unused, available to Phase C).
- **Tests:** a live-PG dead-host routing test (recoverable → `Idle` *not* `Evacuating`; non-recoverable → `Dead`) added to `admin_evac_live_pg.rs` (already in CI's live-PG allowlist); existing `evacuate_dead_source` + drain e2e tests stay green.

This is the bounded-crash-window posture ADR 0042/0043 chose for the *unplanned* case (E2B's explicit-durability stance), accepted because the reactive machinery cost more than the durability it bought.

### Phase B — fork Firecracker

**Status: ✅ DONE — port + pipeline + drop-in proof + self-maintaining cron + prod roll all landed.** #139 landed the maintenance surface (the `node-assets-fetch.sh` `ENGRAM_FC_SRC` consume-seam, the `fc_fork` detect lane, the daily `rebase-fc-fork.yml` cron); #149/#151–#153 the port + vendor + stock↔fork compat test; #158/#159 the catch-up to upstream **v1.16.0**. The fork: `cortexapps/firecracker` branch `engram/live-migration`, a clean-room MAP_SHARED/`Msync` surface (~6 files) on upstream **v1.16.0**, lockstep-tagged `engram-v1.16.0` (`engram-v1.10.1` kept for history). Vendored at `third_party/firecracker`; the `build-firecracker` job (fc_fork-gated, `oras` artifact keyed by fork SHA + the version-agnostic R2 diff-guard) feeds `publish-node-assets` via `ENGRAM_FC_SRC` — green end-to-end on real CI. **Prod-rolled 2026-06-09:** the FC host fleet runs the fork at v1.16.0 (node-assets `5621476`, host-agent #154); all three active images boot + restore fresh v1.16 base snapshots in **~600–780 ms** (smoke-tested in prod), so the fork is a proven drop-in under load, not just in CI.

**Note (this rewrite):** the v1 patch surface (`shared` File-load flag + `Msync`/`MsyncAndState`) was built for the old Phase D and is **superseded by the substrate** — Phase D1 replaces it with the v2 surface (shm-backed guest memory + `MINOR|WP` registration) and repurposes or retires the v1 pieces honestly. Everything else about Phase B — the vendoring, the build pipeline, the R2 guard, the self-maintaining rebase cron, the prod roll — carries forward unchanged and is exactly the infrastructure D1 needs.

**Drop-in proof (M3, dev-vm engram-dev, real KVM):** the forked binary is a verified drop-in for stock FC — the existing `#[ignore]`'d FC suite (`boot`, `snapshot` = Full+File restore, `snapshot_uffd` = Full+UFFD restore ×2, `cross_host_restore`, `lifecycle`) is **green against the fork**, and the new `stock_fork_snapshot_compat` test is **green both directions** (stock-made snapshot restores on the fork *and* fork-made restores on stock — the R2 byte-compat proof). So the fork boots + snapshots + restores cleanly and is interoperable with stock; the irreversible decision is committed and safe. (The compat test is wired into the `test-firecracker` CI job, staging both binaries.)

**Self-maintaining (M4):** `rebase-fc-fork.yml` rewritten into the full track→rebase→build-verify→bump→auto-merge loop — daily it picks the newest stable upstream release (semver-max across all majors by default; `FC_UPSTREAM_BASE` optionally pins a line), rebases the patch branch, **build-verifies before pushing** (a broken rebase never clobbers the good branch), then opens an `fc-fork-bump` engrams PR (via the `engrams-automerge` App, approved from a different identity, auto-merge enabled) that `auto-enqueue.yml` (the generalized ex-`dependabot-auto-enqueue`) enqueues on green. Every green bump auto-merges, majors included; a rebase conflict or clean-rebase build break is loud (tracking issue + red badge). Activated (repo vars set; reuses the cross-org `GH_TOKEN`, no new secret). The fork is **lockstep-tagged** `engram-v<upstream>` (the base-finder uses `--match 'v[0-9]*'` to ignore those tags), so "what FC are we on" is one `git describe`. The first run correctly flagged the `v1.10.1`→current gap; the **catch-up to v1.16.0 is done** (re-port onto v1.16.0's refactored memory backend — `snapshot_memory_to_file` is now in `vstate/vm.rs`, `shared` rides `snapshot_file`, `SNAPSHOT_VERSION` is `10.0.0` and the R2 guard is now version-agnostic; `libseccomp-dev` added to the FC build deps), dev-vm-validated (static drop-in + compat green both ways on v1.16.0). From here the cron handles each release incrementally.

**The v1.10→v1.16 prod roll (the snapshot-format cutover).** FC's snapshot format is **not** cross-version compatible — `SNAPSHOT_VERSION` went `4.0.0` (v1.10.1) → `10.0.0` (v1.16.0), and a 4.0.0 snapshot can't restore on a 10.0.0 binary. So R2's "byte-compatible, mixed-fleet-safe" guarantee is **same-version only**: a roll that crosses a `SNAPSHOT_VERSION` change is a coordinated cutover (drain + base-snapshot re-capture), not a gradual mixed roll. Done in an all-idle/0-user window: bump the HostFleet CR `nodeAssetsImage` → operator drain-roll; mark stale idle sessions dead (their v1.10.1 snapshots can't resume); re-capture each active image's base on v1.16 via `POST /api/v1/enabled-images/refresh`. (Two operational footguns worth recording: an all-digit node-assets SHA tag YAML-coerces to a float in helm values — quote it; and cancelling a `helm upgrade` mid-flight orphans the release in `pending-upgrade` — `helm rollback` clears it.) This surfaced **[#160](https://github.com/cortexapps/engrams/issues/160)**: the coord's base-reuse logic matches on manifest digest + chunk presence but **not** the capturing FC version, so it can reuse a base that's incompatible with the rolled FC (worked around by re-baking the image to change its digest). The M4 cron auto-tracking majors means a `SNAPSHOT_VERSION`-changing release will recur, so #160 should land before the next one (a per-bump SNAPSHOT_VERSION gate on the cron was considered and deferred — handle by watching bump PRs for now). The headline CVEs of the v1.10→v1.16 span (virtio-PCI CVE-2026-5747, jailer CVE-2026-1386) don't affect us — we use neither `--enable-pci` nor the jailer (ADR 0044 K2).

**Implementation divergences (M1/M2):**
- **The surface is `persist.rs`, not `vstate/vm.rs`.** `snapshot_memory_to_file` lives in `persist.rs`; `vm.rs` has no guest-memory dump. The real ~5-file surface: `vstate/memory.rs` (msync + thread `shared` through `from_state`) + `vmm_config/snapshot.rs` (the two variants + top-level `shared` load param) + `persist.rs` (dispatch) + `api_server/{request/snapshot.rs,mod.rs}` + `swagger/firecracker.yaml`. Upstream's `from_raw_regions_file` *already* carries the `shared` MAP_SHARED flag (only `from_state` hardcoded `false`), so the diff is ~140 lines.
- **R2 (snapshot wire-format skew) is a non-issue by construction, not a risk to mitigate.** Upstream serializes state as bincode `SnapshotHdr{magic,version}` + CRC64 with no length prefix (that was the *loopholelabs* design we don't copy). Holding the invariant "never touch `src/vmm/src/snapshot/`, never bump `SNAPSHOT_VERSION`" keeps stock↔fork Full/Diff snapshots byte-compatible both directions → mixed fleets during a roll are safe; no both-hosts-forked gate. Enforced by the `build-firecracker` diff-guard.
- **Build-env (folded into the `build-firecracker` job):** FC pulls `aws-lc-sys` (cmake) + `userfaultfd-sys` (kernel uapi headers + bindgen/clang). The `publish-host-binaries` global `-I/usr/include` CFLAGS can't be reused — it breaks `aws-lc-sys`'s musl build — so expose only the kernel uapi dirs into musl's sysroot. Build only `-p firecracker` (skip jailer/helpers) in `working-directory: third_party/firecracker` so FC's pinned `rust-toolchain.toml` governs.

**Fork-maintenance ergonomics** (a fork is only sustainable if rebasing is automated and breakage is loud): vendored as a git submodule at `third_party/firecracker` (patch surface = a small commit series on the pinned upstream tag); the daily auto-rebase cron (above); the `fc_fork` lane in `.github/scripts/detect-rebake-lanes.py` gating `build-firecracker` + `publish-node-assets`, so bumping the fork is just another change the pipeline rebuilds + rolls.

**Risks (both resolved):** **R1 AGPL** — implemented clean-room from pristine upstream + this ADR/runbook, never the loopholelabs diff; the small surface kept it tractable. **R2 snapshot wire-format skew** — a non-issue *by construction* (above), enforced by the CI diff-guard.

### Phase D — the unified memory substrate

**Status: ◐ S0 spikes ALL PASS (the program's go/no-go — GO); D1 fork v2 is next.**

The substrate as decided above; this section is the implementation ladder. Each milestone is its own PR chain with an ADR bookend update, validated on the real `engram-dev` KVM box with real Firecracker before merge, with every automatically-testable behavior wired into CI (the FC `#[ignore]` suite runs on Blacksmith).

**S0 — feasibility spikes: ✅ ALL PASS (2026-06-09, engram-dev kernel 6.8.0-1060-gcp; prod COS verified 6.12.68+).** The probes live on as the `substrate_kernel_capabilities` CI gate (below). Results:

| Spike | Question | Result |
|---|---|---|
| S0.0 | kernel floors (WP-on-shmem ≥5.19) | ✅ dev VM 6.8, prod COS 6.12.68+ — no upgrades needed |
| S0.1 | `MAP_SHARED` memfd + UFFD registration, CONTINUE, WP delivery | ✅ — with **three protocol findings** (below) |
| S0.2 | the carve: cost, VMA growth, `max_map_count` | ✅ 12–13 µs/carve (incl. content copy); **scattered** +0.8 VMA/carve (20k carves → 16k VMAs), **sequential carves VMA-merge to ~zero growth**; raise `vm.max_map_count` (default 65530) for >~80k-page scattered dirty sets |
| S0.3 | **the go/no-go**: live KVM guest on the substrate, carve under the live memslot | ✅ PASS first run — guest read base (0xAB), wrote through a mid-execution carve (0xEE), re-read base still clean; **guest reads fault as reads (no carve → sharing preserved)**; exactly one carve for exactly the one written page; no KVM/EPT errors |
| S0.4 | fault-storm budget | ✅ direct `CONTINUE` install 0.61 µs/page (**6.2 GiB/s**); blocking single-page fault round-trip 22 µs → engine policy: resolve each fault by CONTINUE-ing the whole surrounding 512 KiB chunk range (one ioctl) ≈ today's per-chunk `UFFDIO_COPY` cost; prefault floods at GiB/s |
| S0.5 | overlay-as-dirty-set vs KVM dirty log | ✅ dirty log == carve set exactly |
| **S0.6** | **the design-freezing follow-up**: the first-round probes ran the handler *in-process*, but production's handler is a **separate process** — and the carve's `mmap(MAP_FIXED)` cannot cross address spaces. Is `MISSING\|MINOR` + cross-process `CONTINUE` + `UFFDIO_COPY` legal on a **`MAP_PRIVATE`** mapping of a shm file? | ✅ **ALL PASS → v2b is the design of record**: cross-process CONTINUE resolves reads against the shared page cache; COPY installs divergent pages privately; guest writes COW natively (file stays clean); sibling mappings share clean base pages. Re-proven under a live KVM guest (OUTs `[ab d7 ee ab]`, exactly one COPY for exactly the divergent page, base file clean, dirty log catches the COW write). |

**S0.1's protocol findings carry into v2b where applicable:** holes don't MINOR-fault (so registration is `MISSING|MINOR` and unpopulated base pages arrive as MISSING → fill by path + CONTINUE); fault-around behavior is moot for correctness in v2b (a pre-mapped clean page COWs on write anyway — privacy is kernel-native, not handler-enforced). The WP/carve protocol (triple registration + upfront `wp_unpopulated` arming + `CONTINUE|MODE_WP`) remains fully proven and gated in CI as **the documented fallback** — superseded only by the process-boundary constraint, not by any kernel failure.

The probes (three self-contained C programs, the exact artifacts the spikes ran) are checked in at `crates/engram-sandbox-firecracker/tests/fixtures/substrate/` and run by the `#[ignore]`'d `substrate_kernel_capabilities` tests, wired into ci.yml's `test-firecracker` job — a kernel/KVM regression surfaces on the runners, not in prod.

**D1 — fork v2 (`cortexapps/firecracker`, clean-room).** On the Uffd restore path (and eventually fresh boot): construct guest memory as `MAP_PRIVATE` of a configured base shm file instead of anonymous, register UFFD `MISSING|MINOR` instead of MISSING-only, and carry the base path on the load request. Handler handshake mappings extended as needed. Snapshot create/read paths verified unchanged (FC reads guest memory through its own mapping; Diff/dirty-log capture proven over this backing by the S0 KVM probe); R2 invariants held (never touch `src/vmm/src/snapshot/`, never bump `SNAPSHOT_VERSION`); the v1 `Msync`/`shared` surface repurposed or retired; the rebase cron stays green (the v2 surface remains a small commit series).

**D2 — handler resolution upgrade (`engram-uffd-handler`).** Smaller than the carve variant would have been: a `CONTINUE` arm for `ResolvedPage::Canonical` (today it COPYs canonical content too), lazy base-shm population by path (canonical chunks only, once per host), `MISSING` vs `MINOR` flag handling, the 512 KiB-chunk-fill / 4 KiB-fault granularity policy (CONTINUE the whole surrounding chunk range per fault — one ioctl), the working-set trace + prefault adapted (prefault becomes range-CONTINUE at 6 GiB/s), install-bitmap semantics revisited. `ResolvedPage::Chunk` keeps today's COPY. Unit tests per arm + an FC integration test in CI.

**D3 — adopt for cold boot: ◐ parity gates PASS, adoption code landed (2026-06-10).** The comparative gate (`substrate_base_create_density_and_latency_parity`, same baked 64 MiB-working-set guest, same snapshot, same host, both arms on the fork): **density Σpss/Σrss 35% (File) vs 35% (substrate)** — identical sharing; **restore median 56 ms (File) vs 61 ms (substrate)**, with the first-ever restore at 154 ms paying the one-time lazy base population. Substrate steady-state RSS is *lower* (76 vs 87 MiB — untouched pages are never resident). Adoption is **derived, not configured**: `effective_restore_mode(fresh)` resolves fresh-creates to substrate-Uffd iff `uffd_base_dir` is set, else File — so a fleet roll flips fresh-create and resume together host-by-host in any order, and `ENGRAM_FC_BASE_RESTORE_MODE` + its env/kill-switch are **retired**. With the substrate on, the eager per-template residency memfile materialization is retired too (base populates lazily by path). Helm carries the dark knob (`firecracker.uffdBaseDir` → node-prep mounts the host-namespace tmpfs via nsenter + the configmap env). **D3 DONE — prod-flipped + canary-validated (2026-06-10, engrams-internal 3994cd4):** fleet drain-rolled onto node-assets b2aa88f (the v2 fork) with `uffdBaseDir=/var/lib/engram/shm`; `Mlocked: 0` on both nodes (issue #148 closed — node RAM 83%→32% on the first node, the rest reclaimable cache); prod canary: instant substrate create, base shm 4 GiB apparent / 49 MiB allocated, exec → snapshot → evict → ~1.6 s resume → state preserved. Dev-stack lifecycle smoke and the capture-of-substrate-VM Full+Diff gate (`capture_of_substrate_vm_round_trips_and_diffs`, in CI) preceded the flip.

**D4 — adopt for resume: ◐ code landed (2026-06-10), prod metrics pending the roll.** Three pieces shipped together: (1) **the canonical split** — `SnapshotMetadata.base_memory_manifest` (serde-default, mixed-roll-safe) carries the IMAGE's base manifest on resume/evac restores, so the handler CONTINUEs base-identical pages against the SHARED per-image base shm — resumed sessions now share with fresh creates (today's resumes share nothing), and resumes stop minting session-keyed base files (fallback `canonical == session` for old coordinators, fresh creates, and reattach). Gated in CI: the revival in `capture_of_substrate_vm_round_trips_and_diffs` asserts exactly one base file. (2) **Resume host-affinity** — `ScheduleContext.prefer_host` (the capturing host, else the session's last host), a soft tier ranked below snapshot-affinity and above capacity, never overriding exclusion/readiness; unit-tested across all precedence edges. (3) **Base-shm GC** — a host-agent sweeper deleting base files that are idle past a grace window AND held open by no process (each handler holds its `BaseShm` fd for exactly the VM's lifetime — a free, precise live-ref signal; the deletion-under-live-mapping hazard is structurally excluded). Hole-punching cold regions within live files remains future work. **Metrics to watch post-roll:** warm same-host resume latency (→ CONTINUE-class), cold cross-host tail (→ eliminated by affinity), PSS/RSS across same-image sessions incl. resumed (→ 35%-class fleet-wide), shm dir occupancy (→ bounded, ~one file per enabled image).

**D5 — teardown pipeline reorder: ◐ code landed (2026-06-10), prod observation pending the roll.** As specified: the host snapshot RPC split into `SnapshotBegin` (pause + drain + FC capture; the guest is re-paused after — today's pipeline already discarded post-capture execution — and the chunk+upload runs as a host-side background task holding the capture lock, so periodic checkpoints keep skipping until it completes) and `SnapshotWait` (awaits the durable metadata). The coordinator's idle eviction marks the session **Idle right after Begin** — user-visible teardown collapses from ~4 s (prod canary measurement) to ≈ the capture — and a finalize task owning the (60 s-touched) session lease awaits the upload, then writes the PG snapshot row (**row-only-at-finalize**: no half-durable rows; any finalize failure = no row + abort + destroy, resume falls back to the prior checkpoint), commits, and destroys. Drain/teleport (target `Evacuating`) deliberately keeps the composed path — the evac scanner resumes from the row, so it must be durable first. Pre-D5 hosts and non-FC backends surface `InvalidSpec` from Begin and fall back to the composed path (mixed rolls safe in any order); resumes landing mid-upload wait up to ~24 s on the lease before the honest 409. **Also resolves issue #147**: the serial re-chunk root cause was already parallelized (ADR 0039), and D5 lands the two open halves — the lease is touched so the 180 s reaper never fires mid-capture (holder-scoped: a touch can't resurrect a reaped lease), and the previously-invisible post phase is instrumented (`engram_snapshot_finish_seconds{type,outcome}`). Gated by: pipeline unit tests (Idle-before-durable; failed-finalize writes no row and still destroys; the legacy composed path regression), the live-PG lease-touch test, and `checkpoint_chain` + the substrate suite on real KVM (the capture/finish refactor preserves the composed `snapshot()` byte-for-byte semantics).

### Phase C — live teleport on the substrate (staged: C1 eager push → C2 post-copy)

**Status: ◐ C1 COMPLETE — shipped, prod-hardened, floor reached (2026-06-11, closing bookend below); C2 IN FLIGHT (opening bookend below — plan of record, staged PR ladder, dev-vm first).** The C1 ladder landed as #174 (local-sink re-chunk + the C2 pagemap spike — PASSED, C2's capture-free dirty map is a GO), #175 (source: capture/export/fetch), #176 (dest: pull/local-manifest restore/catch-up; migration restores FORCE RestoreMode::Uffd), #177 (the coordinator verb), #179 (the two-host e2e on real KVM — the G2 sentinel survives the move), #181 (metrics + the dest-failure kill arm), #182 (the export-TTL dumb-host rule), #183 + #184 (prod-canary fixes: peer addr from the heartbeat-warmed pool, not register-stale PG; batch the per-put cache eviction sweep on staging paths). **Prod canary ledger (sessions 1e1ae394 → a9af111b, hosts a51746c8↔ab021c17):** the live path migrated a real session losslessly — `status:"migrated"`, ZERO `recovered_from_checkpoint` events, row-only-at-finalize durable — with **capture 2.0 s + dest pull/restore 2.4 s ≈ a 4.4 s guest-observed freeze** (vs the same-day snapshot-rehome baseline ~73 s, state-lossy); total wall 25.2 s, dominated by the ~21 s agent handshake on the moved guest — a pre-existing resume-path cost shared with ordinary resumes, named as the next optimization, not a migration mechanism cost. Every failure arm also fired in prod during the canaries and behaved per design: first-move precondition → rehome fallback; dest dial failure → abort-to-source with the session continuing Active (zero loss). G2 met (prod + KVM); G3 arms exercised (unit + two-host + prod); G4 met (finalize <100 ms after catch-up); G1's freeze-window intent met (~4.4 s, vs ≥10× the baseline) with the handshake leg tracked separately.

The D-ladder changed the problem: every host holds the per-image base shm (base pages never transfer), every session owns a forked chain lineage whose divergence is GCS-durable up to the last checkpoint, and the teleport delta is exactly *pages dirtied since the last checkpoint* (cadence-bounded). Today's teleport verb is snapshot-rehome through GCS + a 10 s scanner tick — ~15–30 s end-to-end, and it silently rewinds to the last checkpoint (F1's mislabeled card). Phase C replaces the mechanics under the same verb in two rungs; rung 1 builds almost nothing rung 2 discards.

**The three-class fault taxonomy (the one correction to the original sketch).** The destination's faults partition by *what sources can soundly serve them*: **(1) base-identical** — local `UFFDIO_CONTINUE` against the resident base shm; dissolved by D4, never on the wire. **(2) divergent-but-durable** (in the session manifest at the last checkpoint) — the two-source race exactly as originally sketched: `select!{cache/GCS get_chunk(hash), peer.get_chunk(hash)}`, first SHA-verified wins; legal because both sources hold identical bytes and the dest holds a trustworthy hash. **(3) dirtied-since-checkpoint** — peer-AUTHORITATIVE: GCS's copy is stale and the dest has no valid hash for the live content, so racing GCS is *unsound* (a GCS "win" = silent state-split). Crash-safety for class 3 is the rung-1 whole-VM rewind (loss ≤ cadence), never per-page fallback; `ALT_SOURCE` demotes over-approximated class-3 faults into class 2. The original "every non-ALT_SOURCE fault races GCS in parallel" line was only ever coherent for class 2.

**C1 CLOSING BOOKEND (2026-06-11, the overnight hardening session — commits `1c2c682a..986f38f0`, direct-to-main).** The first prod canaries surfaced a fix ladder far below the migration mechanics; every rung is regression-pinned:

- **Cross-wired NBD device** (#191): the migration restore's inline-manifest attach skipped the sidecar re-point, so the dest FC reopened the *source's* literal `/dev/nbdN` — on a busy dest, *another session's live disk* (corrupt rootfs + cross-session write hazard, masked whenever nbd0 lined up on both sides and by the one-machine e2e until the commit-then-probe NBD-rootfs arm landed). Plus two general data-loss bugs the same e2e caught: `write_chunk`'s RMW ignoring the pending tier (any write inside a flush's drain→rebase window silently dropped the drained content), and FC's writes riding the host block page cache past the capture drain (fsync the device under the pause).
- **The latency ladder**, found via host-visible forensics (`32c1bf83`: handshake sub-leg split + auto-surfacing the handler's jail log on slow handshakes): per-fault `lseek(SEEK_HOLE)` on the base shm (build-once data-range map; `ac6faedc` also pinned the eager sweep to divergent-first/never-zeros/budgeted), the unpublished-manifest prefetch 404 (parse the inline manifest), background pulls losing the resume race (synchronous 8-stream source pull pre-resume, `314ccf11`), and the universal poison: `cache.get`'s populate path running a full eviction sweep per chunk (`f704732e` debounces it — it sat under every handler fault, prefetch, and fetch-fallback in the system).
- **Session-correctness arms**: the harness's reconnect give-up + the silent-EOF-less vsock rebuild (SIGUSR1 nudge from agentd's reattach, indefinite retry), prompts sent inside the freeze window vanishing with the destroyed source (the handler now holds delivery on the session lease and re-resolves post-move — prod-proven, `held_ms=12095` → ran on the dest), the durable-row gate dropped by operator decision (no-row moves proceed; a no-row move failing past the freeze kills the session rather than stranding a scanner-proof zombie), `drain_host` upgraded live-first with rehome fallback, and the status stream curated to `active → evacuating → active` with the closing emit meaning "messages deliver" (C2 will move that emit to immediately-post-blackout).

**Measured floor:** dest handshake 46.6 s → **71 ms**; exec 26 ms post-move; first-hop total ~8–13 s, hop-after-hop ~19 s (a post-restore VM re-dirties its full working set, so the next capture re-chunks it — intrinsic to stop-and-copy). Latency now scales with session churn, not defects: **that is C1's floor**, and the remaining order-of-magnitude is C2's by construction. Comparative note for C2: E2B ships no live migration at all (pause→async-upload→resume-anywhere, with a Redis-registered P2P pull from the pausing host pre-upload — convergent with our export+source-pull); ideas adopted into the C2 plan: origin-affinity on resume placement, the hot-set trace carried in the capture rider (fixes "trace published too late for migrations"), and host-side THP on the base shm (2 MB *guest* pages were subsequently REJECTED — see the correction in the C2 opening bookend).

**Rung C1 — eager dirty-set push (~1–2.5 s moves; zero handler/fork changes).** Synchronous, coordinator-orchestrated, dest-pulls-from-source over one new streaming gRPC on the existing 9101 HostService. Pre-stage dest NVMe from GCS off the clock; then: pause source → NBD drain (pending tier kept local) → FC diff capture → **local-sink re-chunk** (a sink-parameterized `update_for_dirty_ranges_sparse` writing into the source's NVMe `ChunkCache` — no GCS on the pause path) → dest pulls `{state.bin, sidecar, manifest JSONs inline, new memory+disk chunk bytes}` via `MigrationFetch` → dest UFFD-restores (all divergence already in dest NVMe; faults never hit GCS) → PG rebind + harness rebuild → `MigrationCommit` (source destroyed) → dest-side background **durability catch-up** (chunks → manifest → PG row, row-only-at-finalize, the D5 pattern; on failure: no row + the dest drops its chain so the next checkpoint is a safe Full). Because the fresh set moves before resume, there is never a fault with two candidate sources — C1 needs no race at all.

C1 structural decisions: **Evacuating is the crash parachute, not the orchestrator** — transition before pausing, drive synchronously under the (D5-touched) session lease; coordinator death ⇒ lease expiry ⇒ the existing scanner snapshot-rehomes from the last durable row = the rung-1 fallback with zero new recovery code (no new session state, no migrations table in C1; C2 revisits with kill-test evidence). Auth v1 = fleet bearer + single-use TTL'd `export_id` nonce + a source-side artifact allowlist (content-addressing gives integrity; VPC h2c posture unchanged; mTLS/signed tickets = C2 hardening). The **dumb-host export TTL**: no commit/abort within ~120 s ⇒ source asks the coordinator "do I still own session X?" — yes ⇒ un-pause+unfence, no ⇒ destroy, unreachable ⇒ stay paused and retry. Capture **fences** the sandbox (continuous disk-flush publisher + checkpoint driver off until commit/abort — a stale `live_disk_manifest` publish racing the dest is a real split-brain hazard); the dest's publisher starts **paused** until catch-up so it never publishes a manifest referencing non-durable chunks. The disk pending tier *moves* over the same channel (no GCS PUT in the pause window, per B3). Failure arms: source death mid-transfer ⇒ scanner rehome on the pinned dest (loss ≤ cadence); dest death ⇒ `MigrationAbort` un-pauses the source in place (zero loss); plus the required scanner fix — `evac_resumer` must `try_acquire` the session lease and skip if held. New surface: `MigrationCapture/Fetch(stream)/Commit/Abort/Wait` RPCs; a serde-default `MigrationSource` rider on restore metadata (mixed-roll-safe; `InvalidSpec` ⇒ snapshot-rehome fallback, the D5 precedent); `live_migration.rs::migrate_session_live()` behind `ENGRAM_LIVE_TELEPORT=1`, called from `teleport_session` when both hosts advertise capability.

C1 measured gates: **G1** downtime p50 ≤ 2.5 s and ≥10× the same-harness snapshot-rehome baseline (every leg of *today's* teleport instrumented first); **G2** the behavioral differentiator, 20/20 — a sentinel page dirtied AFTER the last checkpoint survives the move (today's path provably loses it), with no `RecoveredFromCheckpoint` event; **G3** every kill arm ends with exactly one bound sandbox, loss ≤ one cadence; **G4** catch-up durable ≤ 30 s p95, never a half-durable row. Riskiest assumption: dest bring-up (FC spawn + UFFD/NBD attach + harness rebuild) ≈ 1 s fixed cost — unmeasured; the escape hatch (pre-spawn the dest FC restore-to-paused *before* pausing the source) is a pure reordering and is exactly C2's pre-stage.

**Rung C2 — true post-copy (after C1 gates + the spike below).** The blackout becomes: pause → **vmstate-only snapshot** (fork v3: a load-params-style flag that skips the memory file entirely; ~40 lines, never touches `src/vmm/src/snapshot/`, no SNAPSHOT_VERSION bump — the only new fork surface) → pagemap scan → `SEAL{chunk bitmap}` + `state.bin` ship → dest load (FC + handler + netns pre-staged) → resume. Honest budget **~30–60 ms guest-observed** (a guest-side monotonic timestamp spinner, never host timers); 10 ms-class is the floor of pause+seal+resume and is gated on FC's load leg shrinking — the gate is the measurement (R6).

C2 mechanics: **the dirty map needs no capture and no KVM-bitmap fork surface** — under the substrate, divergent pages are *anonymous* (COPY-installed or guest-COW) while clean pages are file-backed, so the source host-agent classifies chunks post-pause by walking `/proc/<fc_pid>/pagemap` (file-page bit 61) over the guest VMAs; over-approximation is absorbed by `ALT_SOURCE` (source hashes on request; equal to the durable manifest hash ⇒ "pull it from GCS yourself"). **The page server is the host-agent, not the handler** — the handler's mappings are FC-address-space virtual addresses and YAMA blocks sibling `process_vm_readv`; the host-agent is FC's *parent* (readv allowed), and GPA→hva falls out of `/proc/<pid>/maps` entries backed by the base-shm path (mirroring `GuestRegionUffdMapping`); a readv touching a still-MISSING page triggers the source's own handler fill — correct by construction. Transport for the dest-handler leg: a dedicated TCP listener (e.g. 9102) + length-prefixed frames in a new `engram-migrate-proto` crate (`HELLO/SEAL/NEED_AT/PAGE/ALT_SOURCE/GET_CHUNK/GET_STATE/DRAIN_DONE`) — the client is the deliberately-minimal handler, which shouldn't grow tonic; C1's gRPC fetch remains for host-agent-level artifacts. Dest handler: `--peer-addr/--peer-token/--control-sock` + `peer.rs`; one new branch ahead of `resolve()` (sealed-bitmap offsets ⇒ `NEED_AT`, sha-verify ⇒ COPY; `ALT_SOURCE` ⇒ demote to the class-2 race at `fetch_chunk`); a background drain task reuses the prefault install path; the control sock reports `Sealed/DrainProgress/DrainDone/PeerLost` to the host-agent. Lifecycle: `PostCopySource`/`PostCopyDest` sandbox roles (orphan-reap, idle-evict, flush, and checkpoint drivers must all skip them); **ownership transfers exactly at the `Committing` persist**, and the source never self-resumes after serving `GET_STATE` (the split-brain guard).

**The C2 gate-opener spike (lands inside C1's first PR).** A fourth C probe in the S0 fixtures style: child maps MAP_PRIVATE-of-shm + UFFD `MISSING|MINOR`; one page COPY-installed, one COW-written, one fault-read clean, one untouched; the parent classifies all four via pagemap and `process_vm_readv`s each, asserting class + bytes; then re-run under a live KVM guest (the S0.3 pattern). Graduates into `substrate_kernel_capabilities` as a CI gate. If classification fails, C2 falls back to a KVM-dirty-bitmap fork surface (a bigger v3) — we want that answer before C1 finishes.

**C2 OPENING BOOKEND (2026-06-11 — the plan of record; design review against C1-as-shipped).** The spike PASSED (#174: anon/file/absent classification + readv-through-the-owner's-handler all green), so the capture-free dirty map is the build. Decisions of record, refining the mechanics sketch above:

- **Clean break (operator decision):** C2 *replaces* C1's stop-and-copy arm under the same `ENGRAM_LIVE_TELEPORT=1` gate — no second env var, no standing C1 rung. Capability/precondition failure (pre-roll host, no fork v3, no 9102 listener) ⇒ `InvalidSpec` ⇒ snapshot-rehome, now the only fallback (state-lossy, ≤ cadence). The swap PR deletes the C1 orchestration body + the `MigrationCapture` RPC (no dangling refs); C1's building blocks survive as C2 components (`MigrationFetch`, prestage, the export registry + TTL, the source pull, the `MigrationSourceInfo` rider). Consequence accepted: a C2 defect mid-canary degrades to rehome, not to lossless C1.
- **`GET_STATE` is dropped from the 9102 message list** — state.bin rides C1's `MigrationFetch` (`StateBin` item, already allowlist-gated and dialed warm by prestage); the dest *handler* never needs it. The split-brain guard re-keys accordingly: `MigrationExport.state_served` is set when state.bin leaves the source, and the dumb-host TTL verdict gains a third input — `state_served && still-owned ⇒ StayPaused` (the yes ⇒ un-pause arm is FORBIDDEN once state has shipped). A reattached `PostCopySource` is *never* resumed (ownership false ⇒ destroy; true ⇒ stay paused; converges via the scanner within one cycle). A dest host-agent restart mid-drain reaps the `PostCopyDest` (re-adoption is a recorded follow-up).
- **No new SessionState, no migrations table** — the kill matrix needs neither. The `Committing` persist = one atomic `MetadataStore::rebind_session(session, host, sandbox)` UPDATE replacing the two-step assign, so the ownership oracle (`sandbox_ownership`) flips atomically with the rebind.
- **Fork v3 shape:** a top-level `#[serde(default)] vmstate_only: bool` on `CreateSnapshotParams` (the v2 `uffd_base_file` top-level-param precedent), composing with Full/Diff; `persist.rs` skips the memory dump; `src/vmm/src/snapshot/` untouched, no SNAPSHOT_VERSION bump (R2 holds; the diff-guard proves it). The engrams client method takes **no auto-resume wrapper** — the blackout owns pause/resume, and an auto-resume on the source mid-move is exactly the split-brain.
- **Page-server posture:** one 9102 listener per host, `export_id`-routed (R8's one-in-flight cap makes port contention moot); per-export random token delivered via env, not argv (`/proc/*/cmdline` leaks to same-host users); wire = 4-byte BE length prefix + bincode (the harness-proto/agentd precedent), sync codec so the handler stays tonic-free. Classification adds the **swap bit** over the spike: page dirty = `swapped(62) || (present(63) && !file-backed(61))` — a swapped COW page reads present=0 and would otherwise be silently dropped; absent = clean (never faulted ⇒ the dest's `resolve()` reproduces it from base/manifest/zero). ZEROPAGE-installed chunks classify dirty by construction ⇒ the server's all-zero check answers `ZeroChunk`, never 512 KiB of zeros on the wire.
- **Sequence:** pre-stage everything but the FC load before the pause (netns/TAP, NBD attach, handler spawned in peer mode + HELLO'd, FC up with its API socket bound). Blackout = pause → NBD fsync + dirty-tail drain → vmstate-only create → pagemap scan → SEAL push → state.bin fetch → `PUT /snapshot/load` + resume. Then the `Committing` rebind; the closing `evacuating → active` emit moves HERE (immediately post-blackout — the prompt-hold still parks messages until lease release, so "messages deliver" stays honest); then the finalize task (session lease + the R8 `MigrationGate` both-endpoint guard ride into it): `drain_wait → MigrationCommit` (source destroyed only now). Durability then rides the destination's periodic checkpoint cadence — no post-move `Full` snapshot, no migration-authored PG row. **Source FC + source uffd-handler stay alive (vCPUs paused) until DRAIN_DONE or TTL** — `process_vm_readv` faults still-MISSING pages through the source's own handler, so the page server needs the address space *and* the handler, not running vCPUs. In peer mode the dest handler serves **no fault before SEAL arrives** (FC's load touches guest RAM pre-resume; ordering is enforced in the handler, not assumed of the host-agent).
- **PeerLost is fatal iff sealed class-3 chunks remain undrained** (classes 1–2 demote to base/GCS). The handler latches, reports on the control sock, and exits the fault loop LOUD; the dest host-agent pauses + poisons the `PostCopyDest`; the finalize task (the lease holder — the only actor that can transition without racing the scanner) destroys the dest and re-arms `Evacuating` ⇒ the scanner's rung-1 rewind (loss ≤ cadence). Coordinator-dead fallback: the poisoned dest runs the dumb-host rule against a new `POST .../migration-failed` coordinator endpoint, symmetric with the export TTL.
- **The 12-arm kill matrix** (phases: P1 post-seal/pre-resume, P2 mid-drain, P3 post-DRAIN_DONE/pre-commit; invariant on every arm: exactly one bound sandbox at terminal state): source FC / source agent at P1 ⇒ parachute → scanner rehome (≤ cadence); dest FC / dest agent at P1 ⇒ typed-`NeverLoaded` abort-to-source, Active on source (**zero loss** — the typed error is load-bearing: any post-load or ambiguous failure is parachute, never abort); source FC / source agent at P2 ⇒ PeerLost → rewind (≤ cadence; the agent arm is implemented as a listener-drop and doubles as the network-partition arm); source *handler* at P2 ⇒ resident sealed pages still readv-servable, class-2 misses demote ⇒ drain completes or rewinds (0 / ≤ cadence); dest FC / dest handler / dest agent at P2 ⇒ rewind (≤ cadence; the agent arm via the reattach-reap rule); source at P3 ⇒ harmless — dest self-sufficient, TTL (`owned=false`) cleans the corpse (**zero**); dest agent at P3 ⇒ FC reattaches normally, catch-up state lost ⇒ no row ⇒ next checkpoint is a safe Full (**zero**, the C1 precedent). The coordinator-death soak (lease-expiry timing, multi-minute) stays a dev-vm script, the pre-canary gate. CI asserts a **≤ 500 ms blackout *ceiling*** via a guest monotonic-spinner fixture (Blacksmith nested-virt clocks flake at the 10 ms class); the honest ~30–60 ms number comes only from the dev-vm/prod canary ledger — measure, never quote (R6).
- **E2B folds, resolved:** (1) the **hot-set trace rides the capture rider** — per-jail `--trace-output` on every handler spawn + a one-shot dump when the recorder window closes, `hot_chunks` serde-default on the rider; C1's synchronous source pull sorts hot-first immediately, C2's drain pulls hot∩sealed first. Honesty note: the recorder sees the 5 s post-restore first-fault window — the *resume-time* working set, which is the right predictor for a post-move re-fault storm; a live rolling trace over the control sock is a follow-up. (2) **Origin-affinity on resume: already shipped by D4** (`prefer_host = record.host_id.or(session.host_id)` on the ordinary resume path); the one residual gap is the disk-only cold-boot resume hardcoding `prefer_host: None` through `evacuate_dead_source` — a ~15-line thread-through from that single call site. (3) **Correction: 2 MB *guest* hugepages are REJECTED** (2026-06-11 analysis): 512× coarser COW granularity multiplies divergence — it kills the substrate's density *and* inflates every dirty set (checkpoint, capture, C2 drain) — and C2 demand-faulting wants small transfer units. The TLB/EPT win is pursued as **host-side THP on the base shm** instead, strictly measure-then-flip: a PMD-presence probe (`smaps` FilePmdMapped/ShmemPmdMapped + `/sys/kernel/debug/kvm` largepages) under today's mount vs `huge=within_size`; UFFD registration likely suppresses collapse, so the expected outcome is "fold dead — record and close," and that is a fine answer.
- Recorded follow-ups, not C2 scope: the class-2 race extended to the disk fetch path (re-affirmed from the paragraph below), re-adopting a half-drained dest after a host-agent restart, the live rolling hot-set.

The C2 PR ladder (each rung independently green; dev-vm validation + dev-vm clippy on Linux-gated rungs): fork v3 + submodule bump → `engram-migrate-proto` → the page server (`dirty_map.rs` + `migrate_peer.rs`) → handler `peer.rs` + control sock → the two pre-ladder folds (hot-set rider; origin-affinity micro-gap) → lifecycle roles + TTL v2 → the host-agent orchestration verbs (`migration_prepare_dest` / `migration_capture_postcopy` / `migration_activate_dest` / `migration_drain_wait`, with the `restore_in_jail` prepare/activate split) → the coordinator swap (the clean break) → the headline e2e + spinner → the kill matrix → THP measurement → prod canary + the closing bookend.

**Validation: e2e in CI, not on a checklist.** The Blacksmith `test-firecracker` lane already runs real-KVM FC integration tests, so the two-host shape runs **in CI**: `ENGRAM_INTEG_TWO_HOSTS=1` spins two host-agents in one runner (distinct ports/workdirs/cache roots/jail dirs; transfer over loopback). CI-resident tests: the C1 headline e2e `live_teleport_two_hosts_preserves_post_checkpoint_state` (sentinel dirtied after the last checkpoint survives; no recovery event; downtime recorded), the kill-arm integration tests (`kill_source_mid_transfer_scanner_rehomes_on_pinned_dest`, `kill_dest_mid_restore_source_resumes_lossless`, plus C2's 12-arm matrix — process-level kills work fine on runners), the auth/allowlist negatives, and the fence/publish-gate regressions; for C2, the S-C1 headline `migrate_post_checkpoint_dirty_page_demand_faults_across_p2p` and the guest-observed blackout measurement. Only the coordinator-death soak (lease-expiry timing, multi-minute) stays a dev-vm script, documented as the pre-merge gate. Unit lanes carry the protocol codec, export lifecycle, re-chunk-sink, and scanner-lease tests.

**The disk dirty-tail — SUPERSEDED by disk post-copy (2026-06-12).** The original posture ("drain under pause, bounded by the flush cadence") was measured at **1.3 s of blackout** on the first C2 prod canary (`blackout_disk_drain_ms=1300` vs pause=0/vmstate=23/scan=6): re-chunking every dirty disk chunk (sha256 + cache copy of N×16 MiB) under the freeze, plus the dest staging those chunks over gRPC before `state.bin` could land. Both legs are deleted by giving disk the SAME treatment as memory: under the freeze the capture now only fsyncs the host block cache + quiesces in-flight NBD requests, then **seals the dirty + pending tiers by reference** (zero-copy `Vec→Bytes` moves + refcounted clones — no hashing, no cache writes, no manifest mint). The seal descriptor ships the source's *published* base manifest inline (every hash in it is durable by the `flush_upload` invariant: `base` rebases only after uploads land), so `guest content == manifest ⊕ seal`. The dest rebases its NBD attach to that base and arms a **peer overlay**: reads/writes touching sealed indices demand-fetch raw chunk bytes from the frozen source over the existing `MigrationFetch` stream (new `DiskChunkAt(idx)` item — the seal is its own allowlist; the disk client is the host-agent itself, so no 9102/handler changes) and install into the dest's dirty buffer — exactly the state the source held; a background drain (fan-out 8) pulls the rest; `migration_drain_wait` completes only when **both** planes are self-sufficient. No disk durability work happens at migration time at all — the pulled divergence rides the dest's normal flush cadence (the operator's durability-is-not-migration's-job decision). Mixed-roll safety is fail-loud by construction: the descriptor is a NEW export file (`disk-seal.json`) + NEW item kinds, so an old/new host pairing times out the FC load gate ⇒ `NeverLoaded` ⇒ zero-loss abort-to-source, never a silently-stale disk. Two hazards found during implementation and closed in the same change: (1) the seal **drains** `dirty`, so it is taken *last* in the capture (post-vmstate/scan; the paused guest makes the dirty map stable) with explicit requeue on the two remaining fallible steps, and a fence drop-guard releases `migration_fence` on every pre-export error arm (the pre-existing shape leaked a permanently-fenced backend — silent durability stall — on a capture failure after the disk leg); (2) the dest kernel's udev probe reads the device's first blocks at NBD connect time — *before* the overlay — so the host page cache can hold pre-divergence bytes for sealed chunks (the ext4 superblock region is near-always dirty on a live session); the poller drops it with `BLKFLSBUF` after the overlay install and refuses to land `state.bin` if that fails. The class-2-race extension to the disk fetch path is moot under this design (sealed disk chunks are peer-authoritative, never two-source).

**Restore-tail campaign (2026-06-12, post-disk-post-copy).** Guest-observed blackout (in-guest dual-clock spinner; CLOCK_REALTIME after agentd's PTP step is the honest read — cross-host kvmclock makes MONOTONIC unreliable) went **~554 ms → ~330–345 ms typical** across six measure→fix→re-measure cycles, every theory tested against prod: (1) fetch-poll quantization killed (dial-once + 15 ms poll + 2 ms load gate; the 250 ms poll plus a TCP handshake *per attempt* was pure tax); (2) drain-yields-to-faults shipped but REFUTED as the latency cause (the tight 6–8 ms fault distribution meant a constant, not queueing); (3) per-leg serve attribution (`ServeStats` at DrainDone) ended the theorizing: readv 81 µs, classify-sha 2.47 ms, encode 1.63 ms, write 162 µs per serve; (4) proto v3 `ConnPurpose` — fault connections skip the AltSource classify (a demote on the fault path *adds* a dest-side fetch; strictly worse than shipping resident bytes) → fault 5.9 → 3.1 ms; (5) proto v2 lz4 (sha-over-wire-bytes) + v4 blake3 (no SHA-NI on the fleet; `AltSource.durable_sha256` stays sha256 — content address, not wire integrity) → fault → **2.1 ms avg / 4 ms max**; (6) netlink-first netns (the ADR 0020 P4 parked fix): `netns_provision_ms` **90 → 20** (rtnetlink + throwaway-setns-thread + TUNSETIFF; `ip netns add`/iptables stay subprocess for the bind-mount/teardown contract; netlink-sys needs an entered tokio handle on the setns thread). Hot-first drain ordering shipped (rider trace → `migration-hot-trace.json` → drain leads with hot∩sealed) but the fault count stayed ~120/run — root cause understood: **the rider's hot set is the source's resume-time recorder window, not the move-time working set**, so pages touched after the first 5 s never appear in it. Remaining mass + next levers (fresh-project scale, recorded, not started): a move-time hot set (live rolling trace / pre-capture soft-dirty sampling) to cut fault *count*; FC-load internals (~80–150 ms non-fault, run-to-run dominant variance, needs FC-side spans); tmpfs staging debunked (~5–10 ms — the writes ride the page cache). Local-NVMe for `/var/lib/engram` (engrams-internal#3) targets the post-move *perceived recovery* (dest disk working set at 70–110 ms per 16 MiB pd-ssd hit) and cold boot — not the spinner gap, whose memory path is RAM-to-RAM end to end. **Drain-seam integration** is unchanged from the original sketch: the same `teleport` verb swaps its body behind the capability gate, falling back to snapshot-rehome; K2 reap learns the post-copy roles (R7). **Risks:** R6 measure, never quote; R7 reap coverage; R8 doubled P2P+GCS pressure during consolidation waves ⇒ one in-flight migration per source and per dest host, enforced in `live_migration.rs`.

### Phase E — true autoscale-down

**Status: ● E1 + E2 actuator SHIPPED via ADR 0048 (2026-06-13).** #138 landed the *decision* half (log-only); **ADR 0048 shipped the actuator** and went further than this section anticipated. Rather than reuse `roll_node` + count-only `set_size`, ADR 0048 added `NodePoolScaler::remove_node` (GKE `deleteInstances` — node-specific, decrements the MIG target so no replacement spawns) and a **stateless, teleport-packed consolidation wave** (`autoscale.rs`): cordon+annotate victims, `drain → gate → remove_node → DELETE host`, bounded by `maxShedPerWave`/`maxConcurrentDrains` + a 2D capacity guard. `set_size` became **grow-only**; shrinking is exclusively `remove_node`. Both `IdleOnly` (E1) and `Aggressive` (E2) modes shipped together since the teleport substrate (C2 post-copy) was already live, so the idle-gate relaxation E2 named is just a CRD knob. **Remaining: the engrams-internal staged prod rollout** (the GKE path is unvalidated on real infra until then — ADR 0048 §6).

**Two stages.** **E1 (fork-independent, ships first):** reuse today's snapshot-and-rehome drain, **gated to idle/low-activity hosts** with hard hysteresis — operator-driven cost reclamation, disruptive enough to keep off active hosts. **E2 (after Phase C):** post-copy on the substrate makes the move a near-live teleport, so the idle-gate + hysteresis relax to consolidate *active* hosts continuously — a config loosening, not new mechanism. The substrate sweetens E2 twice: moves are cheap (only divergence travels) **and** packing more sessions per host is what the density half of the substrate exists for.

**The actuation primitive already exists.** `engram-host-operator`'s `roll_node` is already the drain-gated node-removal sequence (cordon → coord drain → gate on `running_sandboxes → 0` → delete pod → uncordon); scale-down reuses its body. **New pieces:** a scale-*down* arm + hysteresis counter in `scaler.rs` `desired_hosts` (shed only when a whole host of slack persists for N consecutive reconciles, clamped to `capacityFloor`/`minHosts`); a `CoordClient::list_hosts()` for the least-loaded victim (`GET /api/hosts` carries per-host `running_sandboxes`); an `AutoscalingSpec.scale_down: Off | IdleOnly | Aggressive` CRD knob (default `Off`, `IdleOnly` for E1, `Aggressive` for E2). **Order:** drain the specific least-loaded node *before* `set_size` (GKE `set_size` is count-only, so draining makes the drained node the safe one to lose). Scale-up stays immediate; scale-down stays slow (the cold-node problem in reverse — a fresh node pays cold chunk-cache reconstruct).

**Risks:** flapping under bursty demand (ship behind the still-off-by-default K4 actuator until calibrated); re-validate the idle-evict ↔ drain race ADR 0044 K5 caught.

### Phase F — admin + web UX for pause / resume / teleport (the test surface)

**Status: ◐ teleport shipped (#137); pause/resume + the live-path swap are pending.** #137 landed the snapshot-rehome teleport end-to-end: `POST /api/admin/sessions/:id/teleport {target_host_id}` marks the session `Evacuating` with the target pinned in `state.teleport_targets`, the scanner resumes it there, and the web `useTeleportSession` host-picker drives it — prod-validated (session `688c54b0`, `ab021c17 → a51746c8`), which surfaced follow-up F1 (below). Pending: the net-new `pause`/`resume` admin passthroughs, and the live-path swap of the `teleport` verb to `migrate_session_live` once Phase C lands (the UX stays put).

A thin tooling layer to drive and observe the live-migration work by hand. `EvacuateSessionRequest` already reserves a target-host field, so "teleport to a chosen host" is a small extension of the existing evacuate pipeline (pause → flush → snapshot → restore on the chosen peer) — pinning the target instead of letting the scanner pick. So F ships an early, **fork-independent** snapshot-rehome form, then the *same* `teleport` verb swaps its implementation to `migrate_session_live` (Phase C). The UX never changes.

- **Admin endpoints:** `POST /api/admin/sessions/:id/teleport {target_host_id}` (extends `evacuate_session` to honor the reserved target); `POST .../pause` + `.../resume` (net-new thin passthroughs to the FC pause/resume primitive — freeze/unfreeze in place, no state-machine change — to exercise the freeze/flush path). Admin-scoped, enforced server-side.
- **Web UX** (`web/`, TanStack Router, cookie auth, admin-gated): `pauseSession`/`resumeSession`/`teleportSession(target)` in `api.ts`; `usePauseSession`/`useTeleportSession` hooks modeled on `useDrainHost`; a small actions group in `SessionDetail`'s `SessionMeta` — Pause/Resume + a Teleport button opening a host-picker that reuses `useHosts()` + the Fleet `Meter`, filtered to ready hosts ranked by free capacity, with the Fleet drain button's confirm pattern.

**Follow-up F1 — the snapshot-rehome teleport mislabels itself as a host failure (found in prod validation).** The early (snapshot-rehome) teleport resumes the session on the target from its *last durable checkpoint*, which lags the live transcript by up to one checkpoint interval (ADR 0043 P2a cadence). That lag triggers the standard rung-1 rewind, which emits `SessionEvent::RecoveredFromCheckpoint` (`engram-coordinator/src/api/snapshot.rs`, `state.rs`). The web renders that event unconditionally via the `Recovery` card (`web/src/components/session-thread/SystemMessage.tsx`) as **"recovered from a checkpoint after a host failure — ~N events after this point were rolled back."** On a planned operator teleport *no host failed* — the operator deliberately moved a healthy session — so the copy is wrong and alarming. (Observed in prod validation: session `688c54b0` teleported `ab021c17 → a51746c8` showed the host-failure card despite a clean move.) **Fix:** distinguish the *cause* of the rewind. Carry a reason on `RecoveredFromCheckpoint` (`PlannedRelocation` vs. `HostFailureRecovery`) — or thread the teleport's `recovery_epoch` provenance — and branch the web copy: a planned move reads "relocated to a new host — ~N events since the last checkpoint were replayed," keeping the honest rolled-back count (snapshot-rehome genuinely drops the post-checkpoint tail) without the false "host failure" framing. **Note:** Phase C dissolves this for planned moves — live post-copy teleport is lossless (no rewind, no rung-1 event), so the card stops firing on planned relocations entirely and "after a host failure" becomes true by construction (it then only ever appears on genuine unplanned recovery). So F1 is a stopgap copy/cause fix for the snapshot-rehome window; C retires the case it patches.

## Interim fallbacks (recorded, not the plan of record)

The design review found two substrate-independent shortcuts. They are deliberately **not** the roadmap — the substrate is the point — but they are real, verified-in-tree options if S0 kills the design or the program must pause:

1. **readv-sourced teleport (no fork, no substrate).** A paused FC's memory is readable by its parent (the host-agent) via `process_vm_readv`/`/proc/pid/mem`, resolving shared base pages and private COW dirty pages alike, for *both* restore modes. Phase C could ship against this source with the two-source combinator unchanged: pause → final diff capture (O(dirty), ~40–200 ms) or direct readv serving → destination UFFD resume. Sub-second teleport, weeks not months — but it inherits today's density-less resume and leaves the memory paths forked. (Caveats verified: YAMA `ptrace_scope` semantics for parent-reads; FC re-parenting on host-agent restart needs `CAP_SYS_PTRACE` as belt-and-braces.)
2. **Teardown pipeline reorder on the diff-chain (no fork).** Split the host snapshot RPC (`snapshot_begin`/`snapshot_wait`), mark `Idle` after the capture leg, upload in the background under a touched lease, write the PG row only at finalize. Collapses user-visible teardown to ≈ pause+capture today, without the substrate. Promoted: this IS D5's design in v2b (the diff-chain stays the capture mechanism).

## Alternatives considered

- **The original Phase D (`MAP_SHARED` memfile + `Msync` flush) as written.** Rejected: unimplementable against prod's actual memory backings (anonymous UFFD RAM; MAP_PRIVATE base) — see "What changed in this rewrite." Its honest repair forks into either D-lite (eager per-sandbox memfile materialization on the latency path) or the substrate; we chose the substrate.
- **Per-sandbox shm backing without the shared base** (`MINOR`-only, no carve). Simpler fork surface and the handler nearly unchanged — but every session privately duplicates its clean working set, un-shipping the ADR 0022/0039 density win. Kept only as the S0.3 hard-failure fallback, with the density cost to be quantified before accepting.
- **Keep snapshot-and-rehome only (no fork).** Faster, no fork burden — but it's the field's commodity behavior, forecloses live teleport and aggressive autoscale-down, and leaves resume density-less forever. Rejected: the substrate is the whole point of the golden state.
- **Fork-of-the-fork (loopholelabs `main-live-migration`).** Archived, far behind upstream, AGPL question, unrelated experiments. Rejected in favor of building the substrate onto upstream ourselves, clean-room.
- **Block-layer memory faulting (silo's model — memory file as an NBD device, no UFFD).** Rejected: we keep our UFFD handler (the fill seam reuses shipped machinery) and borrow only the protocol/durability patterns; silo is AGPL — design reference to reimplement, never import.
- **Network-disk-backed UFFD memory as the per-fault source** (Hyperdisk/NVMe-oF). Rejected as the hot-path source (~ms random-read vs local-NVMe ~µs); viable only as a durable backstop, which the GCS chunk store already is.
- **KSM for density.** Anonymous-only, scan-based (CPU burn + latency), opt-in per-VMA, and defeated by the substrate's shm backing anyway. The `CONTINUE`-shared base is deterministic sharing, not probabilistic dedup.
- **Far / disaggregated memory** (RDMA paging, CXL.mem): research-grade, needs fabric GCP doesn't standardly offer — parked.

## Invariants / tradeoffs (the landmines)

- **The fork is a standing maintenance cost, now with a bigger surface (v2).** Mitigated by the automated daily rebase + loud-failure ergonomics (shipped, Phase B) and by keeping v2 a small auditable commit series. If upstream's memory backend refactors collide, the rebase cron surfaces it before it rots.
- **Carving under a live KVM memslot is the load-bearing kernel assumption.** S0.3 exists precisely to prove or kill it before any production code; its probe graduates into a CI capability test so regressions surface on the runners.
- **VMA growth is bounded but real.** ~2 VMAs per first-written page; raise `vm.max_map_count` deliberately; watch fault-path latency and `/proc/<pid>/maps` size in D2/D3 gates.
- **shm RAM accounting differs from page cache.** The base shm file is swappable but not reclaimable-on-pressure the way the memfile page cache is; hole-punch + retention policy must be explicit (D4), and ADR 0046's memory reservation must count it once, not twice. (Per-session dirty pages are anon COW — accounted exactly as today.)
- **Post-copy source-death race (Phase C).** The window where the destination is live but still faulting from a dying source is the hard case — and exactly where the durable GCS snapshot is the fallback silo/Drafter lack. GCS-fallback must be the *tested default* of the fault path; kill-tests gate every phase.
- **Crash-safety is bounded, not transactional.** Recovery is from the last durable flush, loss ≤ one checkpoint interval. The periodic checkpoint (ADR 0043 P2a) stays the always-on backstop.
- **Density and latency parity are merge gates, not aspirations.** D3/D4 do not ship on a regression; the clean break (retiring File/Uffd) happens only after the gates hold on real images on real KVM.
- **AGPL.** silo/Drafter and the loopholelabs FC branch are AGPL — design reference to *reimplement*, never import; the substrate design is derived from kernel primitives + this ADR. License-read before copying a line.
- **Autoscale-down flapping** (Phase E). Scale up fast, scale down slow; hysteresis + the still-off-by-default actuator until calibrated against real load.

## Open questions (each answered by a named gate)

1. **Does KVM tolerate VMA carving under a live memslot?** → S0.3 (the program go/no-go).
2. **What is the real first-write fault cost and VMA budget at production dirty-set sizes?** → S0.2 + D2 gates.
3. **Does the 4 KiB-fault / 512 KiB-chunk granularity policy hold up under a resume fault storm?** → S0.4 + D4 parity gate.
4. **Real teleport downtime on our network/instance types** (never quote QEMU's) → Phase C kill-test measurements.
5. **P2P bandwidth at density** — doubled P2P+GCS fetch pressure during a consolidation wave; rate-limiting / per-node migration concurrency cap? → Phase C/E2.
6. **Overlay/base retention policy under memory pressure** (warm-resume cache vs RAM headroom; hole-punch cadence) → D4.
7. **Upstreaming any of v2** (shm-backed guest memory + MINOR|WP registration is generically useful) — worth an upstream FC conversation once proven? → after D3.

## Addendum (2026-07-10): base-shm residency must survive host-agent rolls

A 2026-07 production host roll exposed a coherence gap between three substrate pieces that are each correct alone — the C1 pre-warm, the D4 GC, and image readiness:

1. **The C1 pre-warm is skip-if-exists.** A freshly-rolled host-agent pod found the per-image base file already on the node tmpfs (tmpfs mounts outlive the pod), skipped the pre-warm, and marked the image ready. Correct so far — the residency was real.
2. **The D4 sweep's keep-set is liveness, not entitlement.** Its deletion-safety rule (no open FDs + mtime past grace) describes *who is using the file now*, not *who is entitled to it*. VMs outlive a pod roll (ADR 0044 K2) and drain over the following minutes; once the last old-pod handler exited, the fully-populated base file of a **still-enabled** image read as "unreferenced" — and the sweep deleted a couple dozen GiB of accumulated residency.
3. **The readiness recheck verified chunks only.** The 30 s honest-readiness pass re-checks NVMe chunk residency but never noticed the base file vanish, so the host kept advertising ready. The next session create paid full lazy base-shm population at NVMe latency under its own resume fault storm — and latency-budgeted create-path work inside the guest (the ADR 0080 `RefreshAgent` round) blew its deadline, which is how the gap was found.

The C1 rewrite made base-shm residency **part of what `ready_images` advertises**; this addendum makes the rest of the substrate honor that:

- **Enabled-image keep-set for the D4 sweep** (`base_shm_gc::ProtectedPaths`). The image-prefetch supervisor publishes the enabled images' base-file paths (derived from their memory-manifest refs, the same `uffd_base_path_in` naming authority) on every heartbeat-ack reconcile; the sweep never deletes a member. Until the **first** authoritative enabled set arrives, the sweep deletes **nothing** — a fresh pod must not sweep on a view it doesn't have yet.
- **Residency joins the honest-readiness recheck.** When a prefetch left the base file in place (pre-warmed it, or found a roll survivor), readiness records that path; the recheck treats its disappearance exactly like an evicted base chunk — flip to not-ready and re-prefetch, whose pre-warm arm re-creates the now-absent file. Images whose pre-warm was headroom-skipped or failed are deliberately **not** tracked: for them the handler's lazy path is the contract, and gating readiness on a file nothing will write would wedge the image unready forever.

Net effect: a host roll no longer destroys residency (the keep-set preserves it), and any other cause of loss (tmpfs remount, operator `rm`) heals within one recheck interval instead of silently degrading first sessions.

## Sources

Carries forward **ADR 0042** (`docs/adr/0042-substrate-architecture-survey.md` + `0072-substrate-survey-evidence.md`): QEMU post-copy docs + Hines'09; Firecracker snapshot/UFFD docs + discussions #3119/#2938 (FC has no native live migration; UFFD restore is page-source-pluggable); `loopholelabs/drafter` + `silo` v0.2.21 (the unified-device post-copy-with-durable-backstop reference, AGPL — design reference only); REAP/FaaSnap/Catalyzer (working-set prefetch); CodeSandbox engineering blogs (`MAP_SHARED` continuous flush, local-NVMe memory). Kernel references for the substrate: `userfaultfd(2)` + `Documentation/admin-guide/mm/userfaultfd.rst` (MINOR/CONTINUE since 5.13 shmem/hugetlbfs; WP on shmem since 5.19; MISSING scope), `mmap(2)` MAP_FIXED semantics, KVM memslot/GUP behavior under address-space changes. Codebase seams verified against the current tree (the UFFD fill seam, `effective_restore_mode`, the disk flush scheduler, the eviction pipeline + lease, the operator `roll_node` + `desired_hosts`, the node-assets / detect-changes pipeline, the web admin UX). Direct read of the `loopholelabs/firecracker` compare diff happened **only** for the superseded v1 surface (recorded in Phase B history); the substrate (v2) design deliberately derives from kernel primitives alone.

## Status

**Proposed → partially Accepted.** Phase A (#136) and **Phase B** — the fork: port, pipeline, drop-in proof, self-maintaining cron, *and* the prod roll to v1.16.0 — are done and Accepted. The substrate (Phase D) and everything riding it (C, the E1 actuator, E2, F's live path) stay Proposed pending the S0 spikes; the ADR flips fully to Accepted as that commit chain completes.

**Commit chain so far:**
- **#136** — ADR authored + Phase A (retire reactive evac). ✅
- **#137** — Phase F teleport test surface (snapshot-rehome + web host-picker). ◐ (pause/resume + live swap pending)
- **#138** — Phase E1 scale-down decision engine (policy + hysteresis + `ScaleDownMode` knob, log-only). ◐ (actuator pending)
- **#139** — Phase B fork-maintenance ergonomics (consume-seam + `fc_fork` detect lane + daily-rebase cron). ✅
- **#149 / #151–#153** — Phase B fork port + submodule vendor + `build-firecracker` job + stock↔fork compat test. ✅
- **#158 / #159** — Phase B catch-up to upstream **v1.16.0** + the self-maintaining cron (lockstep `engram-v*` tags, version-agnostic R2 guard). ✅ → **prod-rolled 2026-06-09** (fleet on the fork at v1.16.0, all three active images smoke-tested).
- **(this PR)** — the substrate rewrite: Phases C/D re-derived from the unified memory substrate; **S0 spikes run and ALL PASS** (dev VM 6.8 + prod COS 6.12 verified; three fault-engine invariants found and recorded); probes graduated into the `substrate_kernel_capabilities` CI gate.

**Next gate:** **D1 — fork v2** (shm-backed guest memory + `MINOR|WP` registration in `cortexapps/firecracker`), carrying the three S0.1 protocol invariants into the FC/handler design. Open follow-up: [#160](https://github.com/cortexapps/engrams/issues/160) (version-aware base-snapshot reuse), to land before the next `SNAPSHOT_VERSION`-changing FC bump.

Builds on ADR 0042 (survey), ADR 0043 (the shipped no-fork hardening + the precondition Phase 1), ADR 0044 (the K8s fleet whose drain-driven evac Phase A keeps and Phase C upgrades), ADR 0022/0039 (the File-mode density baseline the substrate must meet), and ADR 0028/0038 (the diff-chain durability backstop the substrate keeps).
