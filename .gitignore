/target
/.nix
**/*.rs.bk
*.pdb

# env / secrets
.env
.env.*
!.env.example
*.token
*.pem
*.key
secrets/
secrets.*
credentials.*

# editor
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store

# sqlx offline cache (regenerated)
/.sqlx

# local snapshot/blob storage during dev (root + per-crate test artifacts)
var/
data/

# nix / direnv
result
result-*
/.direnv/
/.envrc.local

# personal Claude Code skills + dev-vm scripts (local-only)
/.claude/
# local-only Claude Code settings in any subproject (e.g. web/.claude/)
settings.local.json
.playwright-mcp/
scratch/
node_modules
__pycache__/
docs/superpowers/**
.impeccable
# On-demand live-model browser evaluation artifacts (ADR 0097).
/artifacts/browser-eval/
