# ADR 0044 K3: HostFleet CRD — GENERATED by engram-host-operator crd. Do not edit by hand;
# regenerate with: cargo run -p engram-host-operator -- crd | (json->yaml).
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: hostfleets.engram.io
spec:
  group: engram.io
  names:
    categories: []
    kind: HostFleet
    plural: hostfleets
    shortNames:
    - hf
    singular: hostfleet
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.image
      name: Image
      type: string
    - jsonPath: .spec.capacityFloor
      name: Floor
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for HostFleetSpec via `CustomResource`
        properties:
          spec:
            properties:
              capacityFloor:
                default: 0
                description: Never drain a node if doing so would drop the fleet below
                  this many schedulable (Ready) hosts. Draining evacuates live sessions
                  onto peers, so there must always be receiving capacity.
                format: uint32
                minimum: 0.0
                type: integer
              coordinatorUrl:
                description: Base URL of the coordinator (e.g. `http://engram-coordinator:8080`).
                  The operator drives cordon/drain on `/api/admin/...` and polls the
                  drain gate on `/api/hosts/:id`.
                type: string
              daemonSet:
                description: Namespace + name of the host-agent DaemonSet this fleet
                  manages.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                - namespace
                type: object
              drainTimeoutSeconds:
                default: 600
                description: Seconds to wait for a drained host's `running_sandboxes`
                  to reach 0 before aborting the roll (leaving the pod in place).
                format: uint64
                minimum: 0.0
                type: integer
              image:
                description: "Target host-agent container image (digest-pinned in\
                  \ prod). A change here is what the operator rolls \u2014 never a\
                  \ registry-tag move."
                type: string
              nodeAssetsImage:
                description: 'Target node-assets image (the `stage-node-assets` init
                  container, ADR 0044 GAP 2). Rolled together with `image`: both ride
                  the pod (per-pod emptyDir), so there''s no swap under a live VM.'
                type: string
            required:
            - coordinatorUrl
            - daemonSet
            - image
            - nodeAssetsImage
            type: object
          status:
            description: Reported progress. v1 leaves this for `kubectl` visibility;
              the operator logs decisions and (future) writes the roll state here
              for a status-driven state machine.
            nullable: true
            properties:
              message:
                description: Last action / blocker, human-readable.
                type: string
              rolledNodes:
                description: Pods already on the target images.
                format: int32
                type: integer
              rollingNode:
                description: The node currently being rolled, if any.
                nullable: true
                type: string
              totalNodes:
                description: Total host-agent pods observed.
                format: int32
                type: integer
            required:
            - message
            - rolledNodes
            - totalNodes
            type: object
        required:
        - spec
        title: HostFleet
        type: object
    served: true
    storage: true
    subresources:
      status: {}
