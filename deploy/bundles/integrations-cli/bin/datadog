#!/bin/sh
set -eu
here="$(dirname "$(readlink -f "$0")")"
export LD_LIBRARY_PATH="$here/../lib${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"
exec "$here/../python/bin/python3" "$here/../libexec/datadog.py" "$@"
