[package]
name = "engram-harness-sdk"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Shared resilient transport and lifecycle primitives for Engram harness adapters"

[lints]
workspace = true

[dependencies]
engram-core.workspace = true
engram-harness-proto.workspace = true
engram-transport.workspace = true
serde_json.workspace = true
tokio = { workspace = true, features = ["io-util", "net", "rt", "signal", "sync", "time"] }
tracing.workspace = true
workspace-hack = { version = "0.1", path = "../../workspace-hack" }
