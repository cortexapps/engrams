[package]
name = "engram-session-bundles"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "ADR 0027: activate read-only host-mounted skill/browser bundles into a session's harness discovery paths. Shared by agentd (FC guest) and the dev ProcessBackend."

[lints]
workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
workspace-hack = { version = "0.1", path = "../../workspace-hack" }

[dev-dependencies]
tempfile = "3"
