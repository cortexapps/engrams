[package]
name = "engram-harness-proto"
version.workspace = true
edition.workspace = true
# Apache-2.0, not the workspace AGPL-3.0: custom harnesses link this crate.
license = "Apache-2.0"
repository.workspace = true
description = "Wire types for the harness ↔ host vsock channel: tool-call events, checkpoint/shutdown commands."

[lints]
workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
async-trait.workspace = true
bincode.workspace = true
chrono.workspace = true
engram-ids.workspace = true
serde.workspace = true
tokio.workspace = true
uuid.workspace = true
workspace-hack = { version = "0.1", path = "../../workspace-hack" }

[dev-dependencies]
# ADR 0099 H4: decode-never-panics property suites (tests/decode_never_panics.rs).
proptest.workspace = true
