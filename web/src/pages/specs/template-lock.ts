/**
 * The reason the template control gives once a spec exists (ADR 0114 D3, R3).
 *
 * The creation sheet states it before the choice; the spec page states it on
 * the disabled control after the choice. One string, so the two never drift.
 */
export const TEMPLATE_LOCK_REASON = "The template is locked once the session starts.";
